{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Attach to Delve",
            "type": "go",
            "request": "attach",
            "mode": "remote",
            "remotePath": "${workspaceFolder}",
            "port": 80,
            "host": "127.0.0.1",
            "program": "${workspaceFolder}/cmd/monitor/main.go",
            "trace": "verbose"
        }
    ]
}